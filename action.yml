name: Cache Go Modules
description: Caches Go Modules
branding:
  icon: save
  color: blue
runs:
  using: composite
  steps:
    # See https://github.com/actions/cache/blob/main/examples.md#linux-1
    - name: Set Up Go Module Cache
      uses: actions/cache@v3
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-
